plugins {
    id 'java'
    id 'application'
    id "com.github.johnrengelman.shadow" version "5.2.0"
}

mainClassName = 'dled.github.com.App'

dependencies {
    implementation 'io.vlingo:vlingo-cluster:0.9.1-RC2'
    implementation 'io.vlingo:vlingo-actors:0.9.1-RC2'

    testImplementation 'junit:junit:4.12'
}

repositories {
    jcenter()
}

shadowJar {
   archiveBaseName = 'vlingo_cluster_experiment'
   classifier = null
   archiveVersion = null
}
